<?xml version="1.0" encoding="UTF-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/annot_ll_btn"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:clickable="true"
    android:orientation="vertical"
    android:visibility="visible"
    android:onClick="onDelete" >   

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="48dp"
        android:layout_gravity="bottom"
        android:layout_weight="0"
        android:background="@drawable/cm_menubar_bg_selector"
        android:orientation="horizontal" >

        <LinearLayout
            android:id="@+id/annot_btn_delete"
            android:layout_width="0dip"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/menu_btn_bg_pushed_selector"
            android:orientation="vertical" >

            <ImageView
                android:id="@+id/annot_btn_delete_image"
                android:layout_width="wrap_content"
                android:layout_height="30dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="5dp"
                android:focusable="false"
                android:src="@drawable/fm_manage_button_delete" />

            <TextView
                android:id="@+id/annot_btn_delete_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:focusable="false"
                android:singleLine="true"
                android:text="@string/cm_btn_delete"
                android:textColor="@color/cm_menubar_text_color"
                android:textSize="9sp" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>